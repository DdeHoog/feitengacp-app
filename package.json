{
  "name": "feitengacp-app",
  "version": "1.0.0",
  "private": true,
  "description": "Combined frontend + backend for the Feitengacp web app.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace=server\" \"npm start --workspace=client\"",
    "build": "npm run build --workspace=client",
    "start": "npm start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
